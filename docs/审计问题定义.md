# 审计问题定义

本文档集中定义 PR/提交信息中引用的审计编号，方便读者不依赖前序审计材料即可理解每个编号的含义。

- **R3-xxx**：第三轮增量审计（`RWR-Vehicle-Studio-Third-Audit-Delta-254629e.md`）新增/复核的问题编号。
- **RV-xxx**：前序深度审计与批次 A–E 的问题编号。
- 状态栏只反映本仓库最近一次提交（`fe90505`）时的处理状态；未处理项仍保留在审计表中。

## R3-xxx

| 编号 | 问题定义 | 状态 |
|---|---|---|
| R3-001 | 增量 Transform 拖拽后 `dirty` / `anyDirty` 不重新计算，可能导致未保存修改丢失 | 已修复 |
| R3-002 | 拖动 turret offset 时只移动 turret 自身对象，依赖它的子炮塔 / turret visual / attached 乘员世界坐标变旧 | 已修复 |
| R3-003 | 首次配置资源后，本次 session 仍会把后续载具当作「从未配置」而反复弹资源窗口 | 已修复 |
| R3-004 | ResourceDialog 在 indexing 中仍可关闭 / 取消，Cancel 无法取消正在进行的 catalog mutation | 已修复 |
| R3-005 | 两个并发 `applyFolders()` 仍可能 latest-call-loses，旧调用后完成会覆盖新调用结果 | 已修复 |
| R3-006 | 删除 root `file` 属性或 Undo root file edit 后，`baseDocument` 不会重新解析 | 已修复 |
| R3-007 | root `<vehicle>` 只能编辑/删除已有属性，不能新增 `file` | 已修复 |
| R3-008 | `revertNode()` 使用 sibling index 作为 saved identity，同级节点增删后会恢复错对象 | 已修复 |
| R3-009 | duplicate basename 已报警，但实际 winner 仍由 WalkDir 未排序的 yield order 决定 | 已修复 |
| R3-010 | `weapon()` 的 `parse_error` 覆盖不了 tolerant parser 产生的结构错误 | 已修复 |
| R3-011 | shield 的 `offset` / `extent` 输入没有使用 strict vec3 validation | 已修复 |
| R3-012 | index 类字段（`turret_index` 等）只验证 Number，没有验证非负整数 | 已修复 |
| R3-013 | `SourceDocument.structuralErrors` 不检测 multiple roots | 已修复 |
| R3-014 | mesh / texture / voxel rejected Promise 永久缓存，外部修复文件后无法重试 | 已修复 |
| R3-015 | 重新建立资源索引不会让 Three.js asset cache / geometry cache 失效 | 已修复 |
| R3-016 | GPU geometry cache app-lifetime 无上限，浏览大量车辆后内存只增不减 | 已修复 |
| R3-017 | 文件树已 lazy，但 schema 仍每次全工作区递归读取；失败时可能残留旧 schema | 已修复 |
| R3-018 | lazy child 加载失败后永远不能在本 session 重试 | 已修复 |
| R3-019 | publish workflow 与 CI 独立并发，测试失败的 commit 仍可能发布 nightly | 已修复 |
| R3-020 | rolling nightly 没有 concurrency，旧 commit 可晚于新 commit 覆盖 release assets | 已修复 |
| R3-021 | 固定 `nightly` tag 不会被 tauri-action 自动移动，tag 仍指向首次 nightly commit | 已修复 |
| R3-022 | Linux release job 缺少当前 Tauri 官方 AppImage pipeline 中的 `patchelf` / `libfuse2` / `file` | 已修复 |
| R3-023 | `packageManager` 声明 `bun@1.3.14`，但 CI/Publish 使用 `latest` | 已修复 |
| R3-024 | single-instance plugin 注册顺序与官方要求相反 | 已修复 |
| R3-025 | weapon session 用 `physicalPath.toLowerCase()` 作为 identity，大小写敏感文件系统上会混淆不同文件 | 已修复 |
| R3-026 | base reference 在 TS 侧按 `/` 与 `\` 分隔，但 Rust `resolve_vehicle_base` 不做跨平台分隔符归一化 | 已修复 |
| R3-027 | saving 中仍可 Discard/Reload，引入保存与放弃之间的状态竞争 | 部分修复：入口已拦截，输入框/gizmo 在保存中仍可编辑 |
| R3-028 | external modification check 失败时 fail-open，默认继续覆盖磁盘 | 已修复：失败时需用户明确确认后才覆盖 |
| R3-029 | discard/reload weapon 不清 `undoStack`，可用 Ctrl+Z 复活已放弃的改动 | 已修复 |
| R3-030 | serialize「含 pending 修改」性能测试实际上主要测 cache hit | 已修复 |

## RV-xxx（本仓库涉及/前序审计项）

| 编号 | 问题定义 | 状态 |
|---|---|---|
| RV-008 | Undo/Revert 入口绑定（Ctrl+Z、恢复本项） | 前序已修复 |
| RV-009 | `revertNode()` 恢复保存快照而非 undo 快照 | 前序已修复 |
| RV-010 | 资源文件夹应用事务化（applyFolders 原子提交） | 前序已修复，本批加固并发 |
| RV-011 | preset draft / active / lastSelection 状态完整 | 前序已修复 |
| RV-012 | 空资源类别处理 | 前序已修复 |
| RV-013 | missing 资源失效与刷新 | 前序已修复，本批补 resource generation |
| RV-014 | ResourceCatalog same-path 快路径与 `scanned` 标志 | 前序已修复 |
| RV-015 | SoldierAssets 身份缓存 | 前序已修复 |
| RV-016 | lazy workspace tree | 前序已修复，本批补失败重试与 schema generation |
| RV-017 | 坏文件降级 `skipped[]` | 前序已修复 |
| RV-018 | duplicate basename 诊断 | 前序已修复，本批补确定性 winner |
| RV-019 | weapon typed `ResourceResult` | 前序已修复 |
| RV-022 | 扫描 warnings 诊断 | 前序已修复 |
| RV-025 | 增量 scene：拖拽直接更 Object3D | 前序已实现，本批修 dirty 与 turret 依赖 |
| RV-026 | GPU geometry 缓存 + 共享体素几何 | 前序已实现，本批补上限与失效 |
| RV-027 | 有界并行加载 | 前序已实现 |
| RV-031 | XML 结构诊断 + 保存保护 | 前序已实现，本批补 multiple roots |
| RV-032 | 严格数值 / vec3 输入 | 前序已实现，本批补整数与 shield |
| RV-034 | root `<vehicle>` 可编辑 | 前序已实现，本批补新增属性与 file 解析 |
| RV-039 | 外部修改检测 | 前序已实现，本批改为失败不默认覆盖 |
| RV-040 | 原子替换（`std::fs::rename`） | 前序已实现 |
| RV-041 | weapon save Rust allowlist 比 vehicle 宽 | 已修复：`save_weapon` 增加 allowed roots |
| RV-042 | raw binary IPC 替代 base64 | 已回退/WAIVED：macOS WKWebView 上 raw 字节 IPC 不可靠，继续使用 base64 |
| RV-043 | Tauri native close-request 拦截 | 已修复：`onCloseRequested` dirty guard |
| RV-044 | SceneController dispose | 已修复 |
| RV-045 | CSP 收紧 | 未处理：需要三平台 GUI 验证，避免误伤 WebView |
| RV-046 | read 路径授权 scope | 未处理：需要与 resource catalog 路径授权一起设计 |
| RV-047 | minWidth 与 CSS 三栏矛盾 | 未处理：当前 `@media 1180px` 三栏合计 935px，`minWidth=1050` 可容纳 |
| RV-048 | build 重复 typecheck | 已修复：`build` 改为 `tauri build` |
| RV-049 | tsconfig 正式 typecheck 不含 tests | 已修复：加入 `tests/**/*.ts` 与 `@types/node` |
| RV-051 | 合成 fixture 让真实资产测试 CI 必跑 | 已修复：新增 `tests/fixtures/` 与 `tests/synthetic-fixtures.test.ts` |
| RV-053 | 旧性能断言过时/不能反映 pending serialize | 已修复：性能测试改为 timed fn 内 toggle 属性 |
| RV-054 | typed hints | 未处理：需先扩展 Rust schema 带类型 |
| RV-055 | 内置 hints | 未处理：需先扩展 Rust schema 带类型/default |
| RV-056 | `console.warn` → 诊断 UI | 未处理 |
| RV-058 | 保存串行化 / saving flag / Save All | 前序已实现，本批补更多入口守卫 |
| RV-059 | saveAs 扩展名 | 当前已有 `ensure_vehicle_extension`，本批未改动 |
| RV-060 | picking 拖动阈值 | 已修复：pointerdown 记起点，pointerup 且位移 <= 5px 才 pick |
| RV-061 | gizmo 字段选择 | 未处理：同一 node 多个 vec3 字段时仍需固定优先级 |
| RV-062 | 内置人物 / 动画资产许可 | DESIGN_RISK：未经确认不改 |
| RV-063 | README `attach_on_turret` typo | 前序已修复 |
| RV-065 | fork canonical 身份 | DESIGN_DECISION：未经确认不改 |
